# Core dependencies for SC Gen 6 - Linux (AMD ROCm/Vulkan)
# Generated for Ubuntu 24.04 with AMD Radeon 8060S (Strix Halo)

# Configuration & Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1

# Document Parsing
PyMuPDF>=1.23.0  # PDF parsing
python-docx>=1.1.0  # DOCX parsing
openpyxl>=3.1.2  # Excel parsing
pandas>=2.1.0  # Data handling
Pillow>=10.1.0  # Image processing
pytesseract>=0.3.10  # OCR (requires Tesseract binary)

# Embeddings & Models
sentence-transformers>=2.3.0  # BGE embeddings
transformers>=4.36.0  # HuggingFace models
accelerate>=0.25.0  # Model acceleration
hf_xet>=1.2.0  # Fast HuggingFace downloads

# PyTorch - Use ROCm version for AMD GPU
--extra-index-url https://download.pytorch.org/whl/rocm6.2
torch>=2.1.0

# ONNX Runtime (CPU fallback on Linux, GPU via llama.cpp Vulkan)
onnxruntime>=1.18.0
onnx>=1.15.0
optimum>=2.0.0

# Vector Database
chromadb>=0.4.22

# Keyword Search
rank-bm25>=0.2.2

# LLM Interface
ollama>=0.1.7
openai>=1.0.0
aiohttp>=3.9.0
requests>=2.31.0

# UI
PySide6>=6.6.0,<6.10.0

# Logging
loguru>=0.7.2

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
markdown>=3.5.0

# Anonymisation (PII detection & pseudonymisation)
presidio-analyzer>=2.2.0  # Microsoft Presidio PII detection
presidio-anonymizer>=2.2.0  # Microsoft Presidio anonymisation operators
spacy>=3.7.0  # NER backbone (also install: python -m spacy download en_core_web_trf)
cryptography>=42.0.0  # Fernet encryption for token registry

# Testing (dev)
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
